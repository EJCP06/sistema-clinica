/**
 * Utilidades para reproducción de audio compatibles con Capacitor.
 *
 * Usa la función `getBackendUrl` que se obtiene de `turnero.util` que
 * resuelve que la URL absoluta del API sea resuelta y entre en uso.
 */

export { getBackendUrl } from '../voz.util'